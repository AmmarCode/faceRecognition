import React from 'react'
import 'tachyons'

const Navigation = () => {
	return (
		<nav style={{display: "flex", justifyContent: "flex-end"}}>
			<p className='f3 link dim pa3 pr4 pointer'>Sign Out</p>
		</nav>
	)
}

export default Navigation
